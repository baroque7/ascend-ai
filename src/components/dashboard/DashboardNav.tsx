'use client';

interface DashboardNavProps {
  currentPage?: string;
}

export default function DashboardNav({ currentPage }: DashboardNavProps) {
  // This component is now minimal - the navigation is handled by the sidebar layout
  // The search bar, notification bell, settings icon, and user profile are in the dashboard page header
  
  return null;
}
